type: object
properties:
  name:
    type: string
    description: Name of the user.
  email:
    type: string
    format: email
    description: Email address of the user.
  password:
    type: string
    format: password
    nullable: true
    description: Password for the user.
required:
  - name
  - email
  - password
